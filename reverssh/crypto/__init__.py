"""Small pure-Python crypto primitives used by reverSSH.

These implementations are dependency-free and covered by known-answer tests.
They are intentionally isolated so a vetted platform implementation can replace
them without touching protocol code.
"""
